# Zerion API key — used SERVER-SIDE only to read the Trading Desk wallet's
# token holdings. Get one at https://developers.zerion.io
# Local: copy this file to ".env" and paste your key below.
# Production: add ZERION_API_KEY in the Vercel project's Environment Variables.
ZERION_API_KEY=
